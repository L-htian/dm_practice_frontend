const Uploader = {
    state: {
        uploadedData: {},
    },
    mutations: {
        set_uploadedData: function (state, data) {
            state.uploadedData = data;
        }
    },
    actions: {}
}
export default Uploader;